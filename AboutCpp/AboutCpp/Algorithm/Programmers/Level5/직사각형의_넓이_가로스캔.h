#pragma once

#include <iostream>
#include <vector>
using namespace std;

long long solution(vector<vector<int> > r)
{
	long long answer = -1;
	
	vector<int> xPosCont;
	vector<int> yPosCont;

	// [실행] 버튼을 누르면 출력 값을 볼 수 있습니다.
	cout << "Hello Cpp" << endl;

	return answer;
}